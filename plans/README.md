# Implementation Plans

Generated by the improve skill on 2026-06-10. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Selected findings**: 1 (baseline), 2 (etapas leak), 3 (API hardening), 6 (QueryClient SSR), 7 (formatted CPF contract — DB stores formatted CPF), 8 (env/docs).

**Not planned** (user declined): 4 (tests), 5 (CI), 9 (dead PROJECT_STEPS), direction spike.

## Execution order & status

| Plan | Title                                       | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Fix red typecheck and lint baseline         | P1       | S      | —          | DONE   |
| 002  | Trim etapas API response to required fields | P1       | S      | 001        | DONE   |
| 003  | Harden projects API (CORS + rate limiting)  | P1       | M      | 001        | DONE   |
| 004  | Create QueryClient per router instance      | P2       | S      | 001        | DONE   |
| 005  | Codify formatted CPF contract end-to-end    | P2       | S      | 001, 003   | DONE   |
| 006  | Add .env.example and fix README onboarding  | P2       | S      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED

## Dependency notes

- **001** must land first — `bun run check-types` and `bun run check` are currently red; later plans assume green gates.
- **002** and **003** can run in parallel after 001.
- **005** follows **003** — CORS/rate-limit middleware should exist before tightening the query schema contract.
- **006** is independent of code changes in 001–005 but should reference the same env var names and ports.

## Findings considered and rejected

- **CPF format mismatch (original #7 framing)**: Maintainer confirmed CPF is stored **formatted** in the DB (`000.000.000-00`). Client already sends formatted values; reframed as plan 005 (codify contract), not a normalization fix.
- **CI workflow (#5)**: Not selected.
- **Unit tests (#4)**: Not selected.
- **Delete PROJECT_STEPS (#9)**: Not selected — may become relevant if a substep detail view is built later.
- **fetch-projects JSON-before-ok check**: Cosmetic; error still surfaces correctly.
- **steps-page staleTime refactor**: Works today; not worth a standalone plan.
